# --------------------------------------
#  tabulating file
#  this file gets called by the aggregation file
#  lives in its own file for decluttering purposes
# --------------------------------------


# A:

d %$% head(table(A1a, exclude = NULL))
d %$% table(is.na(A1a), exclude = NULL)

d %$% table(A1Comments, exclude = NULL)
d %$% table(is.na(A1Comments), exclude = NULL)

d %$% head(table(A2, exclude = NULL))
d %$% table(is.na(A2), exclude = NULL)

d %$% table(A2c_Other, exclude = NULL)
d %$% table(is.na(A2c_Other), exclude = NULL)

d %$% table(A2_Comments, exclude = NULL)
d %$% table(is.na(A2_Comments), exclude = NULL)

d %$% head(table(A3a, exclude = NULL))
d %$% table(is.na(A3a), exclude = NULL)

d %$% head(table(A3b, exclude = NULL))
d %$% table(is.na(A3b), exclude = NULL)

d %$% table(A3Comments, exclude = NULL)
d %$% table(is.na(A3Comments), exclude = NULL)

d %$% head(table(A4a, exclude = NULL))
d %$% table(is.na(A4a), exclude = NULL)

d %$% head(table(A4b, exclude = NULL))
d %$% table(is.na(A4b), exclude = NULL)

d %$% table(A4b_Other, exclude = NULL)
d %$% table(is.na(A4b_Other), exclude = NULL)

d %$% table(A4Comments, exclude = NULL)
d %$% table(is.na(A4Comments), exclude = NULL)

d %$% head(table(A5a, exclude = NULL))
d %$% table(is.na(A5a), exclude = NULL)

d %$% head(table(A5b, exclude = NULL))
d %$% table(is.na(A5b), exclude = NULL)

d %$% head(table(A5c, exclude = NULL))
d %$% table(is.na(A5c), exclude = NULL)

d %$% head(table(A5d, exclude = NULL))
d %$% table(is.na(A5d), exclude = NULL)

d %$% head(table(A5e, exclude = NULL))
d %$% table(is.na(A5e), exclude = NULL)

d %$% head(table(A5f, exclude = NULL))
d %$% table(is.na(A5f), exclude = NULL)

d %$% head(table(A5g, exclude = NULL))
d %$% table(is.na(A5g), exclude = NULL)

d %$% table(A5h_Other, exclude = NULL)
d %$% table(is.na(A5h_Other), exclude = NULL)

d %$% head(table(A5h, exclude = NULL))
d %$% table(is.na(A5h), exclude = NULL)

d %$% table(A5i_Other, exclude = NULL)
d %$% table(is.na(A5i_Other), exclude = NULL)

d %$% head(table(A5i, exclude = NULL))
d %$% table(is.na(A5i), exclude = NULL)

d %$% table(A5j_Other, exclude = NULL)
d %$% table(is.na(A5j_Other), exclude = NULL)

d %$% head(table(A5j, exclude = NULL))
d %$% table(is.na(A5j), exclude = NULL)

d %$% table(A5k_Other, exclude = NULL)
d %$% table(is.na(A5k_Other), exclude = NULL)

d %$% head(table(A5k, exclude = NULL))
d %$% table(is.na(A5k), exclude = NULL)

d %$% table(A5l_Other, exclude = NULL)
d %$% table(is.na(A5l_Other), exclude = NULL)

d %$% head(table(A5l, exclude = NULL))
d %$% table(is.na(A5l), exclude = NULL)

d %$% table(A5Comments, exclude = NULL)
d %$% table(is.na(A5Comments), exclude = NULL)

d %$% head(table(A6a, exclude = NULL))
d %$% table(is.na(A6a), exclude = NULL)

d %$% head(table(A6b, exclude = NULL))
d %$% table(is.na(A6b), exclude = NULL)

d %$% head(table(A6c, exclude = NULL))
d %$% table(is.na(A6c), exclude = NULL)

d %$% head(table(A6d, exclude = NULL))
d %$% table(is.na(A6d), exclude = NULL)

d %$% head(table(A6e, exclude = NULL))
d %$% table(is.na(A6e), exclude = NULL)

d %$% head(table(A6f, exclude = NULL))
d %$% table(is.na(A6f), exclude = NULL)

d %$% head(table(A6g, exclude = NULL))
d %$% table(is.na(A6g), exclude = NULL)

d %$% head(table(A6h, exclude = NULL))
d %$% table(is.na(A6h), exclude = NULL)

d %$% head(table(A6i, exclude = NULL))
d %$% table(is.na(A6i), exclude = NULL)

d %$% table(A6j_Other, exclude = NULL)
d %$% table(is.na(A6j_Other), exclude = NULL)

d %$% head(table(A6j, exclude = NULL))
d %$% table(is.na(A6j), exclude = NULL)

d %$% table(A6k_Other, exclude = NULL)
d %$% table(is.na(A6k_Other), exclude = NULL)

d %$% head(table(A6k, exclude = NULL))
d %$% table(is.na(A6k), exclude = NULL)

d %$% table(A6l_Other, exclude = NULL)
d %$% table(is.na(A6l_Other), exclude = NULL)

d %$% head(table(A6l, exclude = NULL))
d %$% table(is.na(A6l), exclude = NULL)

d %$% table(A6m_Other, exclude = NULL)
d %$% table(is.na(A6m_Other), exclude = NULL)

d %$% head(table(A6m, exclude = NULL))
d %$% table(is.na(A6m), exclude = NULL)

d %$% table(A6n_Other, exclude = NULL)
d %$% table(is.na(A6n_Other), exclude = NULL)

d %$% head(table(A6n, exclude = NULL))
d %$% table(is.na(A6n), exclude = NULL)

d %$% table(A6o_Other, exclude = NULL)
d %$% table(is.na(A6o_Other), exclude = NULL)

d %$% head(table(A6o, exclude = NULL))
d %$% table(is.na(A6o), exclude = NULL)

d %$% table(A6Comments, exclude = NULL)
d %$% table(is.na(A6Comments), exclude = NULL)

d %$% head(table(A7a, exclude = NULL))
d %$% table(is.na(A7a), exclude = NULL)

d %$% head(table(A7b, exclude = NULL))
d %$% table(is.na(A7b), exclude = NULL)

d %$% head(table(A7c, exclude = NULL))
d %$% table(is.na(A7c), exclude = NULL)

d %$% head(table(A7d, exclude = NULL))
d %$% table(is.na(A7d), exclude = NULL)

d %$% head(table(A7e, exclude = NULL))
d %$% table(is.na(A7e), exclude = NULL)

d %$% head(table(A7f, exclude = NULL))
d %$% table(is.na(A7f), exclude = NULL)

d %$% head(table(A7g, exclude = NULL))
d %$% table(is.na(A7g), exclude = NULL)

d %$% head(table(A7h, exclude = NULL))
d %$% table(is.na(A7h), exclude = NULL)

d %$% head(table(A7i, exclude = NULL))
d %$% table(is.na(A7i), exclude = NULL)

d %$% table(A7j_Other, exclude = NULL)
d %$% table(is.na(A7j_Other), exclude = NULL)

d %$% head(table(A7j, exclude = NULL))
d %$% table(is.na(A7j), exclude = NULL)

d %$% table(A7k_Other, exclude = NULL)
d %$% table(is.na(A7k_Other), exclude = NULL)

d %$% head(table(A7k, exclude = NULL))
d %$% table(is.na(A7k), exclude = NULL)

d %$% table(A7l_Other, exclude = NULL)
d %$% table(is.na(A7l_Other), exclude = NULL)

d %$% head(table(A7l, exclude = NULL))
d %$% table(is.na(A7l), exclude = NULL)

d %$% table(A7m_Other, exclude = NULL)
d %$% table(is.na(A7m_Other), exclude = NULL)

d %$% head(table(A7m, exclude = NULL))
d %$% table(is.na(A7m), exclude = NULL)

d %$% table(A7n_Other, exclude = NULL)
d %$% table(is.na(A7n_Other), exclude = NULL)

d %$% head(table(A7n, exclude = NULL))
d %$% table(is.na(A7n), exclude = NULL)

d %$% table(A7o_Other, exclude = NULL)
d %$% table(is.na(A7o_Other), exclude = NULL)

d %$% head(table(A7o, exclude = NULL))
d %$% table(is.na(A7o), exclude = NULL)

d %$% table(A7Comments, exclude = NULL)
d %$% table(is.na(A7Comments), exclude = NULL)

d %$% head(table(A8a, exclude = NULL))
d %$% table(is.na(A8a), exclude = NULL)

d %$% head(table(A8b, exclude = NULL))
d %$% table(is.na(A8b), exclude = NULL)

d %$% head(table(A8c, exclude = NULL))
d %$% table(is.na(A8c), exclude = NULL)

d %$% head(table(A8d, exclude = NULL))
d %$% table(is.na(A8d), exclude = NULL)

d %$% head(table(A8e, exclude = NULL))
d %$% table(is.na(A8e), exclude = NULL)

d %$% head(table(A8f, exclude = NULL))
d %$% table(is.na(A8f), exclude = NULL)

d %$% head(table(A8g, exclude = NULL))
d %$% table(is.na(A8g), exclude = NULL)

d %$% head(table(A8h, exclude = NULL))
d %$% table(is.na(A8h), exclude = NULL)

d %$% head(table(A8i, exclude = NULL))
d %$% table(is.na(A8i), exclude = NULL)

d %$% table(A8j_Other, exclude = NULL)
d %$% table(is.na(A8j_Other), exclude = NULL)

d %$% head(table(A8j, exclude = NULL))
d %$% table(is.na(A8j), exclude = NULL)

d %$% table(A8k_Other, exclude = NULL)
d %$% table(is.na(A8k_Other), exclude = NULL)

d %$% head(table(A8k, exclude = NULL))
d %$% table(is.na(A8k), exclude = NULL)

d %$% table(A8l_Other, exclude = NULL)
d %$% table(is.na(A8l_Other), exclude = NULL)

d %$% head(table(A8l, exclude = NULL))
d %$% table(is.na(A8l), exclude = NULL)

d %$% table(A8m_Other, exclude = NULL)
d %$% table(is.na(A8m_Other), exclude = NULL)

d %$% head(table(A8m, exclude = NULL))
d %$% table(is.na(A8m), exclude = NULL)

d %$% table(A8n_Other, exclude = NULL)
d %$% table(is.na(A8n_Other), exclude = NULL)

d %$% head(table(A8n, exclude = NULL))
d %$% table(is.na(A8n), exclude = NULL)

d %$% table(A8o_Other, exclude = NULL)
d %$% table(is.na(A8o_Other), exclude = NULL)

d %$% head(table(A8o, exclude = NULL))
d %$% table(is.na(A8o), exclude = NULL)

d %$% table(A8Comments, exclude = NULL)
d %$% table(is.na(A8Comments), exclude = NULL)

d %$% head(table(A9a, exclude = NULL))
d %$% table(is.na(A9a), exclude = NULL)

d %$% head(table(A9b, exclude = NULL))
d %$% table(is.na(A9b), exclude = NULL)

d %$% head(table(A9c, exclude = NULL))
d %$% table(is.na(A9c), exclude = NULL)

d %$% head(table(A9d, exclude = NULL))
d %$% table(is.na(A9d), exclude = NULL)

d %$% head(table(A9e, exclude = NULL))
d %$% table(is.na(A9e), exclude = NULL)

d %$% head(table(A9f, exclude = NULL))
d %$% table(is.na(A9f), exclude = NULL)

d %$% head(table(A9g, exclude = NULL))
d %$% table(is.na(A9g), exclude = NULL)

d %$% head(table(A9h, exclude = NULL))
d %$% table(is.na(A9h), exclude = NULL)

d %$% head(table(A9i, exclude = NULL))
d %$% table(is.na(A9i), exclude = NULL)

d %$% table(A9j_Other, exclude = NULL)
d %$% table(is.na(A9j_Other), exclude = NULL)

d %$% head(table(A9j, exclude = NULL))
d %$% table(is.na(A9j), exclude = NULL)

d %$% table(A9k_Other, exclude = NULL)
d %$% table(is.na(A9k_Other), exclude = NULL)

d %$% head(table(A9k, exclude = NULL))
d %$% table(is.na(A9k), exclude = NULL)

d %$% table(A9l_Other, exclude = NULL)
d %$% table(is.na(A9l_Other), exclude = NULL)

d %$% head(table(A9l, exclude = NULL))
d %$% table(is.na(A9l), exclude = NULL)

d %$% table(A9m_Other, exclude = NULL)
d %$% table(is.na(A9m_Other), exclude = NULL)

d %$% head(table(A9m, exclude = NULL))
d %$% table(is.na(A9m), exclude = NULL)

d %$% table(A9n_Other, exclude = NULL)
d %$% table(is.na(A9n_Other), exclude = NULL)

d %$% head(table(A9n, exclude = NULL))
d %$% table(is.na(A9n), exclude = NULL)

d %$% table(A9o_Other, exclude = NULL)
d %$% table(is.na(A9o_Other), exclude = NULL)

d %$% head(table(A9o, exclude = NULL))
d %$% table(is.na(A9o), exclude = NULL)

d %$% table(A9Comments, exclude = NULL)
d %$% table(is.na(A9Comments), exclude = NULL)

d %$% head(table(A10a, exclude = NULL))
d %$% table(is.na(A10a), exclude = NULL)

d %$% head(table(A10b, exclude = NULL))
d %$% table(is.na(A10b), exclude = NULL)

d %$% head(table(A10c, exclude = NULL))
d %$% table(is.na(A10c), exclude = NULL)

d %$% head(table(A10d, exclude = NULL))
d %$% table(is.na(A10d), exclude = NULL)

d %$% head(table(A10e, exclude = NULL))
d %$% table(is.na(A10e), exclude = NULL)

d %$% table(A10f_Other, exclude = NULL)
d %$% table(is.na(A10f_Other), exclude = NULL)

d %$% head(table(A10f, exclude = NULL))
d %$% table(is.na(A10f), exclude = NULL)

d %$% table(A10g_Other, exclude = NULL)
d %$% table(is.na(A10g_Other), exclude = NULL)

d %$% head(table(A10g, exclude = NULL))
d %$% table(is.na(A10g), exclude = NULL)

d %$% table(A10h_Other, exclude = NULL)
d %$% table(is.na(A10h_Other), exclude = NULL)

d %$% head(table(A10h, exclude = NULL))
d %$% table(is.na(A10h), exclude = NULL)

d %$% table(A10Comments, exclude = NULL)
d %$% table(is.na(A10Comments), exclude = NULL)

d %$% head(table(A11a, exclude = NULL))
d %$% table(is.na(A11a), exclude = NULL)

d %$% head(table(A11b, exclude = NULL))
d %$% table(is.na(A11b), exclude = NULL)

d %$% head(table(A11c, exclude = NULL))
d %$% table(is.na(A11c), exclude = NULL)

d %$% head(table(A11d, exclude = NULL))
d %$% table(is.na(A11d), exclude = NULL)

d %$% head(table(A11e, exclude = NULL))
d %$% table(is.na(A11e), exclude = NULL)

d %$% head(table(A11f, exclude = NULL))
d %$% table(is.na(A11f), exclude = NULL)

d %$% head(table(A11g, exclude = NULL))
d %$% table(is.na(A11g), exclude = NULL)

d %$% table(A11h_Other, exclude = NULL)
d %$% table(is.na(A11h_Other), exclude = NULL)

d %$% head(table(A11h, exclude = NULL))
d %$% table(is.na(A11h), exclude = NULL)

d %$% table(A11i_Other, exclude = NULL)
d %$% table(is.na(A11i_Other), exclude = NULL)

d %$% head(table(A11i, exclude = NULL))
d %$% table(is.na(A11i), exclude = NULL)

d %$% table(A11j_Other, exclude = NULL)
d %$% table(is.na(A11j_Other), exclude = NULL)

d %$% head(table(A11j, exclude = NULL))
d %$% table(is.na(A11j), exclude = NULL)

d %$% table(A11k_Other, exclude = NULL)
d %$% table(is.na(A11k_Other), exclude = NULL)

d %$% head(table(A11k, exclude = NULL))
d %$% table(is.na(A11k), exclude = NULL)

d %$% table(A11Comments, exclude = NULL)
d %$% table(is.na(A11Comments), exclude = NULL)




# B:

d %$% head(table(B1a, exclude = NULL))
d %$% table(is.na(B1a), exclude = NULL)

d %$% head(table(B1b, exclude = NULL))
d %$% table(is.na(B1b), exclude = NULL)

d %$% head(table(B1c, exclude = NULL))
d %$% table(is.na(B1c), exclude = NULL)

d %$% table(B1d_Other, exclude = NULL)
d %$% table(is.na(B1d_Other), exclude = NULL)

d %$% head(table(B1d, exclude = NULL))
d %$% table(is.na(B1d), exclude = NULL)

d %$% table(B1e_Other, exclude = NULL)
d %$% table(is.na(B1e_Other), exclude = NULL)

d %$% head(table(B1e, exclude = NULL))
d %$% table(is.na(B1e), exclude = NULL)

d %$% table(B1_Comments, exclude = NULL)
d %$% table(is.na(B1_Comments), exclude = NULL)

d %$% head(table(B2a, exclude = NULL))
d %$% table(is.na(B2a), exclude = NULL)

d %$% head(table(B2b, exclude = NULL))
d %$% table(is.na(B2b), exclude = NULL)

d %$% head(table(B2c, exclude = NULL))
d %$% table(is.na(B2c), exclude = NULL)

d %$% head(table(B2d, exclude = NULL))
d %$% table(is.na(B2d), exclude = NULL)

d %$% table(B2e_Other, exclude = NULL)
d %$% table(is.na(B2e_Other), exclude = NULL)

d %$% head(table(B2e, exclude = NULL))
d %$% table(is.na(B2e), exclude = NULL)

d %$% table(B2f_Other, exclude = NULL)
d %$% table(is.na(B2f_Other), exclude = NULL)

d %$% head(table(B2f, exclude = NULL))
d %$% table(is.na(B2f), exclude = NULL)

d %$% table(B2g_Other, exclude = NULL)
d %$% table(is.na(B2g_Other), exclude = NULL)

d %$% head(table(B2g, exclude = NULL))
d %$% table(is.na(B2g), exclude = NULL)

d %$% table(B2_Comments, exclude = NULL)
d %$% table(is.na(B2_Comments), exclude = NULL)

d %$% head(table(B3a, exclude = NULL))
d %$% table(is.na(B3a), exclude = NULL)

d %$% table(B3_Comments, exclude = NULL)
d %$% table(is.na(B3_Comments), exclude = NULL)

d %$% head(table(B4a, exclude = NULL))
d %$% table(is.na(B4a), exclude = NULL)

d %$% head(table(B4b, exclude = NULL))
d %$% table(is.na(B4b), exclude = NULL)

d %$% table(B4c_Other, exclude = NULL)
d %$% table(is.na(B4c_Other), exclude = NULL)

d %$% head(table(B4c, exclude = NULL))
d %$% table(is.na(B4c), exclude = NULL)

d %$% head(table(B5a, exclude = NULL))
d %$% table(is.na(B5a), exclude = NULL)

d %$% head(table(B5b, exclude = NULL))
d %$% table(is.na(B5b), exclude = NULL)

d %$% table(B5c_Other, exclude = NULL)
d %$% table(is.na(B5c_Other), exclude = NULL)

d %$% head(table(B5c, exclude = NULL))
d %$% table(is.na(B5c), exclude = NULL)

d %$% head(table(B6a, exclude = NULL))
d %$% table(is.na(B6a), exclude = NULL)

d %$% head(table(B6b, exclude = NULL))
d %$% table(is.na(B6b), exclude = NULL)

d %$% table(B6c_Other, exclude = NULL)
d %$% table(is.na(B6c_Other), exclude = NULL)

d %$% head(table(B6c, exclude = NULL))
d %$% table(is.na(B6c), exclude = NULL)

d %$% head(table(B7a, exclude = NULL))
d %$% table(is.na(B7a), exclude = NULL)

d %$% head(table(B7b, exclude = NULL))
d %$% table(is.na(B7b), exclude = NULL)

d %$% table(B7c_Other, exclude = NULL)
d %$% table(is.na(B7c_Other), exclude = NULL)

d %$% head(table(B7c, exclude = NULL))
d %$% table(is.na(B7c), exclude = NULL)

d %$% table(B4_7Comments, exclude = NULL)
d %$% table(is.na(B4_7Comments), exclude = NULL)

d %$% head(table(B8a, exclude = NULL))
d %$% table(is.na(B8a), exclude = NULL)

d %$% table(B8Comments, exclude = NULL)
d %$% table(is.na(B8Comments), exclude = NULL)

d %$% head(table(B9a, exclude = NULL))
d %$% table(is.na(B9a), exclude = NULL)

d %$% head(table(B9b, exclude = NULL))
d %$% table(is.na(B9b), exclude = NULL)

d %$% table(B9c_Other, exclude = NULL)
d %$% table(is.na(B9c_Other), exclude = NULL)

d %$% head(table(B9c, exclude = NULL))
d %$% table(is.na(B9c), exclude = NULL)

d %$% head(table(B10a, exclude = NULL))
d %$% table(is.na(B10a), exclude = NULL)

d %$% head(table(B10b, exclude = NULL))
d %$% table(is.na(B10b), exclude = NULL)

d %$% table(B10c_Other, exclude = NULL)
d %$% table(is.na(B10c_Other), exclude = NULL)

d %$% head(table(B10c, exclude = NULL))
d %$% table(is.na(B10c), exclude = NULL)

d %$% head(table(B11a, exclude = NULL))
d %$% table(is.na(B11a), exclude = NULL)

d %$% head(table(B11b, exclude = NULL))
d %$% table(is.na(B11b), exclude = NULL)

d %$% table(B11c_Other, exclude = NULL)
d %$% table(is.na(B11c_Other), exclude = NULL)

d %$% head(table(B11c, exclude = NULL))
d %$% table(is.na(B11c), exclude = NULL)

d %$% head(table(B12a, exclude = NULL))
d %$% table(is.na(B12a), exclude = NULL)

d %$% head(table(B12b, exclude = NULL))
d %$% table(is.na(B12b), exclude = NULL)

d %$% table(B12c_Other, exclude = NULL)
d %$% table(is.na(B12c_Other), exclude = NULL)

d %$% head(table(B12c, exclude = NULL))
d %$% table(is.na(B12c), exclude = NULL)

d %$% table(B9_12Comments, exclude = NULL)
d %$% table(is.na(B9_12Comments), exclude = NULL)

d %$% head(table(B13a, exclude = NULL))
d %$% table(is.na(B13a), exclude = NULL)

d %$% table(B13Comments, exclude = NULL)
d %$% table(is.na(B13Comments), exclude = NULL)

d %$% head(table(B14a, exclude = NULL))
d %$% table(is.na(B14a), exclude = NULL)

d %$% head(table(B14b, exclude = NULL))
d %$% table(is.na(B14b), exclude = NULL)

d %$% head(table(B14c, exclude = NULL))
d %$% table(is.na(B14c), exclude = NULL)

d %$% table(B14d_Other, exclude = NULL)
d %$% table(is.na(B14d_Other), exclude = NULL)

d %$% head(table(B14d, exclude = NULL))
d %$% table(is.na(B14d), exclude = NULL)

d %$% table(B14e_Other, exclude = NULL)
d %$% table(is.na(B14e_Other), exclude = NULL)

d %$% head(table(B14e, exclude = NULL))
d %$% table(is.na(B14e), exclude = NULL)

d %$% table(B14f_Other, exclude = NULL)
d %$% table(is.na(B14f_Other), exclude = NULL)

d %$% head(table(B14f, exclude = NULL))
d %$% table(is.na(B14f), exclude = NULL)

d %$% table(B14Comments, exclude = NULL)
d %$% table(is.na(B14Comments), exclude = NULL)

d %$% head(table(B15a, exclude = NULL))
d %$% table(is.na(B15a), exclude = NULL)

d %$% head(table(B15b, exclude = NULL))
d %$% table(is.na(B15b), exclude = NULL)

d %$% table(B15c_Other, exclude = NULL)
d %$% table(is.na(B15c_Other), exclude = NULL)

d %$% head(table(B15c, exclude = NULL))
d %$% table(is.na(B15c), exclude = NULL)

d %$% head(table(B16a, exclude = NULL))
d %$% table(is.na(B16a), exclude = NULL)

d %$% head(table(B16b, exclude = NULL))
d %$% table(is.na(B16b), exclude = NULL)

d %$% table(B16c_Other, exclude = NULL)
d %$% table(is.na(B16c_Other), exclude = NULL)

d %$% head(table(B16c, exclude = NULL))
d %$% table(is.na(B16c), exclude = NULL)

d %$% head(table(B17a, exclude = NULL))
d %$% table(is.na(B17a), exclude = NULL)

d %$% head(table(B17b, exclude = NULL))
d %$% table(is.na(B17b), exclude = NULL)

d %$% table(B17c_Other, exclude = NULL)
d %$% table(is.na(B17c_Other), exclude = NULL)

d %$% head(table(B17c, exclude = NULL))
d %$% table(is.na(B17c), exclude = NULL)

d %$% head(table(B18a, exclude = NULL))
d %$% table(is.na(B18a), exclude = NULL)

d %$% head(table(B18b, exclude = NULL))
d %$% table(is.na(B18b), exclude = NULL)

d %$% table(B18c_Other, exclude = NULL)
d %$% table(is.na(B18c_Other), exclude = NULL)

d %$% head(table(B18c, exclude = NULL))
d %$% table(is.na(B18c), exclude = NULL)

d %$% table(B15_18Comments, exclude = NULL)
d %$% table(is.na(B15_18Comments), exclude = NULL)



# C:

d %$% head(table(C1a, exclude = NULL))
d %$% table(is.na(C1a), exclude = NULL)

d %$% head(table(C1b, exclude = NULL))
d %$% table(is.na(C1b), exclude = NULL)

d %$% head(table(C1c, exclude = NULL))
d %$% table(is.na(C1c), exclude = NULL)

d %$% head(table(C1d, exclude = NULL))
d %$% table(is.na(C1d), exclude = NULL)

d %$% head(table(C1e, exclude = NULL))
d %$% table(is.na(C1e), exclude = NULL)

d %$% table(C1f_Other, exclude = NULL)
d %$% table(is.na(C1f_Other), exclude = NULL)

d %$% head(table(C1f, exclude = NULL))
d %$% table(is.na(C1f), exclude = NULL)

d %$% table(C1g_Other, exclude = NULL)
d %$% table(is.na(C1g_Other), exclude = NULL)

d %$% head(table(C1g, exclude = NULL))
d %$% table(is.na(C1g), exclude = NULL)

d %$% table(C1h_Other, exclude = NULL)
d %$% table(is.na(C1h_Other), exclude = NULL)

d %$% head(table(C1h, exclude = NULL))
d %$% table(is.na(C1h), exclude = NULL)

d %$% table(C1Comments, exclude = NULL)
d %$% table(is.na(C1Comments), exclude = NULL)

d %$% head(table(C2, exclude = NULL))
d %$% table(is.na(C2), exclude = NULL)

d %$% table(C2Comments, exclude = NULL)
d %$% table(is.na(C2Comments), exclude = NULL)

d %$% head(table(C3, exclude = NULL))
d %$% table(is.na(C3), exclude = NULL)

d %$% table(C3Comments, exclude = NULL)
d %$% table(is.na(C3Comments), exclude = NULL)

d %$% head(table(C4a, exclude = NULL))
d %$% table(is.na(C4a), exclude = NULL)

d %$% head(table(C4b, exclude = NULL))
d %$% table(is.na(C4b), exclude = NULL)

d %$% table(C4c_Other, exclude = NULL)
d %$% table(is.na(C4c_Other), exclude = NULL)

d %$% head(table(C4c, exclude = NULL))
d %$% table(is.na(C4c), exclude = NULL)

d %$% table(C4d_Other, exclude = NULL)
d %$% table(is.na(C4d_Other), exclude = NULL)

d %$% head(table(C4d, exclude = NULL))
d %$% table(is.na(C4d), exclude = NULL)

d %$% table(C4Comments, exclude = NULL)
d %$% table(is.na(C4Comments), exclude = NULL)

d %$% head(table(C5a, exclude = NULL))
d %$% table(is.na(C5a), exclude = NULL)

d %$% head(table(C5b, exclude = NULL))
d %$% table(is.na(C5b), exclude = NULL)

d %$% head(table(C5c, exclude = NULL))
d %$% table(is.na(C5c), exclude = NULL)

d %$% head(table(C5d, exclude = NULL))
d %$% table(is.na(C5d), exclude = NULL)

d %$% head(table(C5e, exclude = NULL))
d %$% table(is.na(C5e), exclude = NULL)

d %$% head(table(C5f, exclude = NULL))
d %$% table(is.na(C5f), exclude = NULL)

d %$% head(table(C5g, exclude = NULL))
d %$% table(is.na(C5g), exclude = NULL)

d %$% head(table(C5h, exclude = NULL))
d %$% table(is.na(C5h), exclude = NULL)

d %$% head(table(C5i, exclude = NULL))
d %$% table(is.na(C5i), exclude = NULL)

d %$% head(table(C5j, exclude = NULL))
d %$% table(is.na(C5j), exclude = NULL)

d %$% head(table(C5k, exclude = NULL))
d %$% table(is.na(C5k), exclude = NULL)

d %$% head(table(C5l, exclude = NULL))
d %$% table(is.na(C5l), exclude = NULL)

d %$% head(table(C5m, exclude = NULL))
d %$% table(is.na(C5m), exclude = NULL)

d %$% head(table(C5n, exclude = NULL))
d %$% table(is.na(C5n), exclude = NULL)

d %$% table(C5o_Other, exclude = NULL)
d %$% table(is.na(C5o_Other), exclude = NULL)

d %$% head(table(C5o, exclude = NULL))
d %$% table(is.na(C5o), exclude = NULL)

d %$% table(C5p_Other, exclude = NULL)
d %$% table(is.na(C5p_Other), exclude = NULL)

d %$% head(table(C5p, exclude = NULL))
d %$% table(is.na(C5p), exclude = NULL)

d %$% table(C5q_Other, exclude = NULL)
d %$% table(is.na(C5q_Other), exclude = NULL)

d %$% head(table(C5q, exclude = NULL))
d %$% table(is.na(C5q), exclude = NULL)

d %$% table(C5r_Other, exclude = NULL)
d %$% table(is.na(C5r_Other), exclude = NULL)

d %$% head(table(C5r, exclude = NULL))
d %$% table(is.na(C5r), exclude = NULL)

d %$% table(C5s_Other, exclude = NULL)
d %$% table(is.na(C5s_Other), exclude = NULL)

d %$% head(table(C5s, exclude = NULL))
d %$% table(is.na(C5s), exclude = NULL)

d %$% table(C5t_Other, exclude = NULL)
d %$% table(is.na(C5t_Other), exclude = NULL)

d %$% head(table(C5t, exclude = NULL))
d %$% table(is.na(C5t), exclude = NULL)

d %$% table(C5u_Other, exclude = NULL)
d %$% table(is.na(C5u_Other), exclude = NULL)

d %$% head(table(C5u, exclude = NULL))
d %$% table(is.na(C5u), exclude = NULL)

d %$% table(C5v_Other, exclude = NULL)
d %$% table(is.na(C5v_Other), exclude = NULL)

d %$% head(table(C5v, exclude = NULL))
d %$% table(is.na(C5v), exclude = NULL)

d %$% table(C5Comments, exclude = NULL)
d %$% table(is.na(C5Comments), exclude = NULL)
